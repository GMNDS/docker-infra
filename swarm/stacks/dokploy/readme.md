## Instruções

Por conta de como o dokploy funciona está hardcoded no código que o nome do service pra ele conseguir capturar os logs precisa ser dokploy
então como esse é uma instalação  manual existe o script `create-service.sh` pra criar os services a partir do `stack.yml`
