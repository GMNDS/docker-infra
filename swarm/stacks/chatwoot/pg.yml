secrets:
  chatwoot_postgres_password:
    external: true

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_PASSWORD_FILE=/run/secrets/chatwoot_postgres_password
      - TZ=America/Sao_Paulo
      - PG_MAX_CONNECTIONS=500
    secrets:
      - chatwoot_postgres_password
    networks:
      - traefik_proxy
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ${DOCKER_ROOT}/volumes/chatwoot/init_pg:/docker-entrypoint-initdb.d
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
         # - node.hostname == worker1
      # resources:
      #   limits:
      #     cpus: "1"
      #     memory: 3072M

volumes:
  postgres_data:
    external: true
    name: postgres_data

networks:
  traefik_proxy:
    external: true
